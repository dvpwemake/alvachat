/* Real backend URL (Cloudflare Worker, free).
   After `npx wrangler deploy` in /worker, paste the printed URL here.
   Example: "https://alvachat-api.yourname.workers.dev"
   Leave "" to use same-origin /api (local python app.py). */
window.MEET50_API = "";
